/*
import java.util.Scanner;

public class TestFind {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.println("press 3 integer ");

        get file;
        print to screen;
IO.stream.out(socket('127.0.0.1', '3000'), "<HTML><><></HTML>")
        wait(threa1,thread2){
            find(firstInt,secondInt,lastInt);
        }
    }

    A (){
        File('A.txt').addLock()
        file = open File('A.txt')
    file.lastline.write("hello")
        int firstInt =sc.nextInt();
        File('A.txt').releaseLock();
    }

    B (){
        if (File('A.txt').checkLock()){
            setTimer(wait 1 sec(File('A.txt').checkLock())
        } else {
            File('A.txt').write('world')
        }

        file = open File('A.txt')
        file.lastline.write("world")
        int firstInt =sc.nextInt();
    }



    public static void find(int firstInt,int secondInt,int lastInt){
        if(firstInt>secondInt&&firstInt>lastInt){
            System.out.println(firstInt+" bigges`t");}

        else if(secondInt>firstInt&&secondInt>lastInt){
            System.out.println(secondInt+" biggest");}

        else{System.out.println(lastInt+" biggest");}

    }
}
*/
